# AmericanSignLanguageDetector
Real-time American Sign Language (ASL) Alphabet Recognition using MediaPipe Hand Landmarks and Deep Learning. The system collects hand landmark data, trains a neural network, and performs live ASL prediction via webcam.
